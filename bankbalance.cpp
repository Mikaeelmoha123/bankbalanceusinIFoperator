#include <iostream>

using namespace std;

int main()
{
    int age;
    float bankBalance;
    cout << "Enter your age: ";
    cin >> age;
    if (age >= 22)
    {
        cout << "Proceed to key in bank balance: ";
        else if (age < 22)
                cin >>
            bankBalance;
        if (bankBalance > 50, 000)
    }
}